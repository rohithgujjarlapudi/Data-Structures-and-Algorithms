#include <stdio.h>

int main ()
{
 int data,i,n=5;
  int ar[5]={1,2,3,4,5};
  data=3;
  for(i=0;i<n;i++)
    {
      if(data==ar[i])
      {
        printf("number is at index %d",i);
      }
      
        }
  
  return 0;
}
